security:
  oauth2:
    client:
      client-id: clientId
      client-secret: secret
      grant-type: client_credentials
      scope: read
      access-token-uri: http://localhost:9001/oauth/token

app:
  oauth2:
    serviceId:
      client-id: clientId
      client-secret: secret
      grant-type: client_credentials
      scope: read
      access-token-uri: http://localhost:9001/oauth/token